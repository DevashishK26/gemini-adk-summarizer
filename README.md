# gemini-adk-summarizer
Production-ready AI agent built with Google ADK and Gemini, deployed on Cloud Run. Exposes an HTTP API to summarize text into concise insights with key points and word count, featuring structured JSON output, session handling, and scalable serverless deployment.
